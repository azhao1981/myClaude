请扮演一位 **Anthropic 官方 Prompt Engineering 专家**。我要你对我的一段 Prompt 进行深度技术评估和优化。

**目标对象**：
请读取路径为 `{你的提示词文件路径}` 的文件。
*(如果没有文件，请提示我直接粘贴 Prompt 内容)*

请执行以下分析步骤：

1.  **结构扫描 (Structural Analysis)**
    * **XML 标签使用**：检查是否使用了 `<instructions>`, `<context>`, `<examples>`, `<input>` 等 XML 标签来清晰分隔数据和指令？（这是 Claude 模型理解复杂指令的关键）。
    * **角色设定 (Persona)**：是否定义了清晰的角色（System Role）？
    * **示例 (Few-Shot)**：是否提供了具体的输入/输出示例（Examples）？

2.  **思维链检查 (Chain of Thought)**
    * **CoT 触发**：Prompt 是否指示模型在回答之前进行“思考”？（例如："Please think step-by-step inside <thinking> tags before answering."）
    * **推理引导**：对于复杂任务，是否引导了推理步骤？

3.  **清晰度与边界 (Clarity & Constraints)**
    * **否定约束**：是否明确说了“不要做什么”？
    * **边缘情况**：是否处理了输入缺失或错误的情况？

4.  **最终优化报告 (Output)**
    请输出一份 Markdown 报告：
    * **评分 (0-10分)**：基于 Anthropic 最佳实践打分。
    * **🔍 问题诊断**：(例如："所有内容混在一起，模型很难区分哪里是背景，哪里是指令。")
    * **✨ 优化后的 Prompt**：请根据你的分析，**重写**这个 Prompt。重写版必须包含：
        * 合理的 XML 结构。
        * `<thinking>` 思考引导。
        * 更清晰的指令分层。

---
请开始读取并分析。